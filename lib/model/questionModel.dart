class questionModel{
  int? numberOfQuestion, numberOfCorrectAnswer;
  String ? question;
  List<String> answers = [];
  questionModel(this.numberOfQuestion, this.question, this.answers, this.numberOfCorrectAnswer);
}